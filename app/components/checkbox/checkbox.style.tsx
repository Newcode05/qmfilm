import { tv } from "tailwind-variants";

export const CheckBoxStyle = tv({
  base: "inline-block w-[18px] h-[18px] flex justify-center items-center rounded-[2px] border border-black",
  variants: {},
});
